/**
 * div - Divides two integers
 * @a: First integer (dividend)
 * @b: Second integer (divisor)
 *
 * Return: Result of division
 */
int div(int a, int b)
{
if (b == 0)
{
return (0);
}
return (a / b);
}
